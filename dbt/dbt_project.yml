name: 'fabric_epex'
version: '1.0.0'
config-version: 2

profile: 'fabric_epex'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  fabric_epex:
    +materialized: view
